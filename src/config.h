#include <chrono>
using namespace std::chrono;

const bool line_wrapping 	= true; /* Not working right now */
const bool horizontal_line_wrap = true;

const bool numbered_lines  = false;
const char empty_line_char = '~';

const bool tab_spaces = true;
const int  tab_width  = 12; /* Only for tab_spaces = true*/

const bool auto_save 	      = false;
const auto auto_save_interval = 5s;
